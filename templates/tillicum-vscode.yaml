# Run job for 8 hours so we can work interactively with vscode remote ssh all day
resources:
  infra: slurm/tillicum
  accelerators: H200:1

config:
  slurm:
    sbatch_options:
      account: gaia
      time: "8:00:00"

setup: |
  pwd
  ls
  df -h
  free -h

run: |
  nvidia-smi
